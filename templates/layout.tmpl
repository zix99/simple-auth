<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
        <title>{{block "title" .}}Simple Auth{{end}}</title>
    </head>
    <body>
        <section class="section">
            <div class="container">
                {{template "main" .}}
                <p class="subtitle">
                    Simple-Auth &copy; 2020
                </p>
            </div>
        </section>
    </body>
</html>